# Local secrets for `wrangler dev` — never commit these.
.dev.vars
.dev.vars.*
.env
.env.*

node_modules/
.wrangler/

*.log
.DS_Store

# 审计相关的产物：brief、报告、复现脚本、打包件。它们是围绕某一次审计的工作文件，
# 不是这个项目的组成部分 —— 直接交给人，不进仓库。
AUDIT*.md
audit-evidence/
*-audit-*.zip
