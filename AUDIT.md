# 独立审计指引

> 这份文件是给**另一个全新 session** 的。写它的人（上一个 session）同时写了这个项目的
> 代码、测试和 README —— 也就是说，这三样东西全都建立在同一套可能有错的假设上。
> 你的价值恰恰在于你没有那套假设。

---

## 1. 你的任务

对这个仓库做一次**独立**审计，目标是找出会让它**该停机时没停机**、或者**不该停机时停了**的缺陷。

「独立」的具体含义：

- **README 是待审对象，不是依据。** 里面每一个推导、每一张表格里的数字，都是写代码那个 agent 自己算的，没有第二个人核过。**把它当成一组待验证的声明。**
- **代码注释同理。** 注释里写「所以取 60 分钟」的地方，请自己把那个数重算一遍。
- **测试通过不构成证据。** 测试和夹具出自同一套假设；假设错了，它们会一起错得很自信。这个仓库里真出过这种事（见第 5 节）。
- **不要因为某处有很长的注释解释「为什么这样做」就跳过它。** 解释得越充分的地方，越可能是当初想复杂了、或者想岔了的地方。

先读代码，再读 README，最后对照。不要反过来。

---

## 2. 三十秒了解这个项目

一个 Cloudflare Worker，cron 每 10 分钟跑一次，查 AWS Lightsail 实例本月已用流量，超线就调 `StopInstance`，下个月额度重置后调 `StartInstance`。防的是 Lightsail 没有上限的流量超额账单。

两道停机线：

- **静态线**：月度总量 ≥ 额度 × `THRESHOLD`
- **突发闸门**：按最近半小时的速率算，剩余额度撑不过一个「反应视野」

无状态，无 UI，不主动通知。核心文件是 `src/index.js`（约 630 行）。

```bash
npm test          # 78 个用例
npm run mutate    # 变异测试：注入 19 个已知缺陷，确认测试会红（约 3-5 分钟）
node --test --experimental-test-coverage
npx wrangler deploy --dry-run --outdir /tmp/x
```

---

## 3. 用户的实际优先级

按这个顺序权衡，不要按「代码好不好看」：

1. **绝不能刷出超额账单**（漏停的代价 >> 多停的代价）
2. **也不希望被过早停机**（合法的大流量传输不该被掐）
3. **不希望高频调用上游 API 被判定为滥用**（这是 cron 取 10 分钟而不是更快的唯一理由）
4. 适用范围锁定在「整个 AWS 账号只有一台长期存在的实例」

---

## 4. 不要重新讨论的事

这些是用户已经拍板的，重新论证只是浪费时间：

- **只支持单实例。** 多实例、混合规格、跨区域、月中创建的按天折算额度——全部明确不做。
- **cron 用 10 分钟。** 不是因为额度不够（额度绰绰有余），是不想显得像在滥用接口。
- **没有运行时的「临时关闭保护」开关。** 这是刻意的。
- **不主动发通知。** 靠 AWS Budgets 和 Lightsail 原生告警补，这两道在 README 里是「必做」。
- **文档正文用中文，日志与错误消息用英文。**

如果你认为其中某条本身就是错的，可以在报告末尾单独提，但不要花主要精力在这里。

---

## 5. 重点怀疑对象

按「如果错了，后果多严重 × 有多可能错」排。每一条都是**问句**，不是待确认的结论。

### 5.1 那些没有被任何人核过的算式

`src/index.js` 的注释和 README 里散布着一堆推导，全部出自同一个人的一次性计算：

- 检测回路 = 桶关闭 + 落库延迟 + cron 间隔 + 停机生效，以及由它推出的「视野要 ≥ 2 倍回路」
- 突发闸门跳闸位置的闭式解：`runway = (t_exh − L) × H / (H + W − L)`——**这个公式是手推的，没有被数值模拟验证过**
- 「延迟容忍上限 = 视野/2 − 回路固定部分 = 840 秒」
- README「上游 API」一节那张暴露量表（5 Gbps 下不同落库延迟烧掉多少 GiB）
- 「1 Gbps 越线后最坏再烧约 164 GiB」

**建议做法**：别去读推导，直接写一个离散时间模拟器（按秒推进：流量累积、桶按 300 秒关闭、数据延迟 L 后可见、cron 每 600 秒评估一次、停机延迟 60 秒生效），跑出实际烧掉多少，和上面的解析结果对照。对不上的地方就是发现。

### 5.2 对 AWS API 行为的假设，哪些没有被核实过

代码里有若干处依赖 Lightsail `GetInstanceMetricData` 的具体行为。上一个 session 核过其中一部分（响应字段类型、空响应的两种长相、错误状态码），但**至少有一条是纯推理**：

> 月度查询用 `period: 86400`、`startTime` 取 UTC 月初。这假定 Lightsail 会把日粒度的桶**对齐到 UTC 零点**。这个行为是从 CloudWatch 的语义推过来的，**没有对 Lightsail 本身查证过**。如果它实际是按 `startTime` 起算的滑动桶，或者有别的对齐方式，月初至今的总量就可能多算或少算一整天。

请自己列一遍代码对上游行为的全部假设，逐条标注「已查证 / 未查证」，并优先攻击未查证的那些。可用 AWS 官方文档；如果有 AWS 凭据，直接调一次真实 API 比读十页文档更有说服力。

### 5.3 测试的不变量是不是把需求抄错了

`test/invariants.test.js` 断言的是「从需求推出来的」不变量。但**推它们的人和写实现的人是同一个**——如果他对需求的理解本身有偏差，不变量会和实现一起错，而且看起来完全自洽。

请**先不看测试**，自己从 README 第 3 节（用户优先级）和项目目标出发列一遍「这个东西必须永远成立的性质」，再去和 `invariants.test.js` 比对。差集就是发现。

### 5.4 变异列表是想象力的边界

`scripts/mutation-test.sh` 里的 19 个变异体现的是**上一个 session 能想到的缺陷**。它想不到的那些，正好就是它看不见的。

请自己往里加变异——尤其是那些「代码照样跑、行为看着对、但防线变薄」的类型（改常量、改比较边界、改分支顺序、把 `await` 去掉、把 `Promise.all` 改成串行、把两个方向的指标名对调……）。**有变异存活就是发现。**

注意脚本自己的正确性也要验：它必须先跑对照组（干净代码不能报红），且失败路径要真的返回非零（`INVARIANT_CASES=1 bash scripts/mutation-test.sh` 应当退出 1）。

### 5.5 这个架构本身有没有想复杂

突发闸门是后加的，加进来之后就再没有人质疑过它是否值得。请认真考虑：

- 它拦的场景（Gbps 级突发）在上游 API **16 分钟的不可压缩盲区**面前，真的拦得住吗？README 自己承认那一档「正确做法是在实例前面加限速」。
- 如果它拦不住，那它带来的复杂度（额外两次 API 调用、一个观察窗口、一个反应视野、一个延迟容忍上限、以及这四者之间的耦合）是否划算？
- 反过来：有没有更简单的方案能达到同样效果？比如把 `THRESHOLD` 调低是否就够？

**这是一个真正开放的问题，不要预设答案是「值得」。**

### 5.6 数字漂移

README 刚刚被整体重写过。重写前的版本里，有 **6 处数字**在多轮修补中过期而没被发现（比如「每月约 89,000 次调用」实际是 17,856）。

请把 `src/index.js` 的常量、`wrangler.jsonc` 的 cron、测试里的硬编码值、README 里的每一个数字**交叉核一遍**。`test/tuning.test.js` 只钉住了常量之间的**关系**，钉不住文档里的数字。

### 5.7 已知未验证项（确认它们是否仍然未验证）

上一个 session 声称下面三件事「还没有被线上验证过」。请确认这个说法是否仍然成立，以及是否还有别的类似项被漏掉：

1. 日志里 `meter ... behind` 这个字段是否真的会出现（依赖 Lightsail 返回的时间戳是 Unix 数字）
2. 指标落库延迟的实际分布（整套余量标定的唯一未文档化输入，设计成立的条件是「< 约 18 分钟」）
3. `StopInstance` 打在一台已经停机的实例上会返回什么（fail-closed 路径可能走到）

---

## 6. 方法建议

上一个 session 连续几轮 review 每次都有新发现，原因是它每次只换一个视角。以下是有效的做法，以及必须遵守的纪律。

**有效的做法**

- **写模拟器 / 探针，不要只靠读代码。** 这个项目里几乎每个真缺陷都是探针跑出来的，不是看出来的。
- **对着上游官方文档逐条核夹具。** 最严重的一个缺陷（会让停机的实例整月无法重启）就是夹具把 AWS 的**合法**空响应当成了畸形响应。
- **注入已知缺陷，确认测试会红。** 不会红的测试等于没有。
- **查「常量之间的耦合」有没有一起走。** 改一个忘了另一个时，代码照样跑、测试照样绿。

**必须遵守的纪律**（每一条都是上一个 session 踩过的坑，而且每次的表现都是**静默地一律输出「通过」**）

1. **你的验证脚本本身也要被验证。** 至少要有：对照组（无缺陷时不能报警）、反向用例（有缺陷时必须报警）。
2. **变异 / 注入实验全程在临时副本里做，绝不碰工作区。** 曾经有一次中断把 `if (false && ...)` 留在了 `src/` 里。
3. **不要用 `grep -c` 数 node reporter 的失败行**（它打两遍），也不要把 `node --test | grep -q` 放在 `set -o pipefail` 下（管道会返回 node 的退出码而不是 grep 的，检测逻辑会被整个反过来），也不要用 `.` 去匹配 `ℹ` `✔` 这类多字节字符。
4. **做完任何实验，`git diff` 确认工作区干净再收工。**

---

## 7. 交付

一份报告，包含：

1. **发现清单**，每条给出：复现方式（命令或脚本）、影响（会导致漏停还是误停）、你判断的严重程度。
2. **你核实过但没问题的清单**——这个同样重要，它划定了这次审计的覆盖边界。
3. **你没能核实的清单**，以及为什么（缺凭据？缺上游文档？需要真实流量？）。
4. 如果你认为某处架构选择本身有问题，单独一节。

**报告里请避免把「测试通过」「覆盖率 100%」作为结论的依据。** 请说明你实际验证了什么、用什么方法验证的。

---

## 8. 不要做的事

- 不要为了让测试变绿而改测试的期望值——先确认到底是实现错了还是期望错了。
- 不要直接 push。发现和改动分开：先报告，改动由用户决定。
- 不要重新论证第 4 节里那些已经拍板的事。
