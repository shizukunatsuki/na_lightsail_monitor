# 独立审计报告 · na-lightsail-monitor

**审计对象**：提交 `d54b2ed`
**日期**：2026-08-22
**状态**：工作区未改动；78 个既有用例全部通过（**这不构成本报告任何一条结论的依据**）

核心结论：注释和 README 里的闭式解是错的；突发闸门对最坏情况没有贡献；而落库延迟超过 25 分钟之后闸门会静默失效——日志那时会写 `now 0 kbps`。

| 严重 | 中等 | 轻微 | 核实无误 | 无法核实 |
| --- | --- | --- | --- | --- |
| 2 | 3 | 5 | 21 | 4 |

---

## 1. 方法，以及对方法本身的检验

这份报告不把「测试通过」或「覆盖率」当证据。下面每一条结论都对应一个可以重跑的实验。

主要工具是一个**离散事件模拟器**：它把 `src/index.js` 里真实的 `worker.scheduled` 挂到一条模拟的 Lightsail 指标管道上——底层 300 秒样本、桶关闭后延迟 L 才可发布、cron 每 600 秒落在时间格上、`StopInstance` 60 秒后真正断流——然后直接量「实例断流那一刻真实烧掉了多少字节」。不重新推导任何公式。

### 模拟器自己也要被检验

建模过程中我自己踩了两个坑，都是靠自检发现的：

1. 第一版把底噪从月初一路积到突发起点，与 `priorUsedBytes` 重复计数了 292 GiB，静态线在突发之前就被触发。
2. 第二版对 `period=300` 的查询返回了**部分填充**的桶，而真实的 5 分钟样本是全有或全无。

两次都表现为「一整片格子结果完全相同」。

最终版本带 7 项自检：

- 两个对照组（零流量、温和流量都不得停机）
- 两个反向用例（越线必停、Gbps 突发必停）
- 两个刻度校验（无延迟时静态线几乎不超冲、检测滞后落在 `[660, 1560]` 秒）
- 一项**交叉校验**——把注入的 L 与被审代码自己算出来的 `meter … behind` 对比，五档全部落在预期区间。如果我的时间戳夹具和代码的理解不一致，这一项会立刻报错。

此外用了三条辅助路径：一套**独立于模拟器**的最小复现（直接打桩 `fetch`）；一批**追加的变异体**（16 个，与仓库里那 19 个不重合）；以及对 `aws4fetch` 源码和 AWS 官方 API 文档的逐条核对。

---

## 2. 发现清单

按「如果错了后果多严重」排序。

### F1 — 落库延迟越过 25 分钟后，突发闸门静默失效，而日志断言 `now 0 kbps`

**严重 · 漏停**

可观测延迟存在一个**上限**：窗口是 30 分钟、粒度 300 秒，所以落在窗口里的最旧数据点其桶结束时刻距今最多 `1800 − 300 = 1500` 秒 = 25 分钟。延迟再大一点，窗口里**一个点都没有**，于是：

- `newest` 为 `null` → `lagSeconds` 为 `null` → `stale` 恒为 `false` → **`BLIND` 永远不会写**；
- `meter … behind` 字段整个消失（缺失的字段，很难拿来告警）；
- `rateOf` 在 `points === 0` 时返回 `0`，这个 0 被当成正常读数打进 `OK` 行。

结果是：延迟 25 分钟时看门狗**大声报警并成功停机**；延迟 26 分钟时它写下一行 `OK`，并**主动断言实例没有流量**。

```
延迟    BLIND?   停机?   日志
 25 min  是        是       ... STOPPED | burning 5726.6 Mbps with 724.000 GiB of quota left = 18 min to overage
 26 min  否 ←      否 ←     ... OK | used 300.000 GiB | stop at 819.200 GiB | now 0 kbps, never to quota | month 47% elapsed
 45 min  否 ←      否 ←     ... OK | used 300.000 GiB | stop at 819.200 GiB | now 0 kbps, never to quota | month 47% elapsed
```

> 同一场突发（每个 5 分钟桶 200 GiB），只改落库延迟。`node blind-cliff.mjs`

这恰好违反项目自己写在 README 和代码注释里的原则——「*读不懂的响应一律抛错，绝不折算成 0；0 字节是唯一会让看门狗什么都不做的读数*」。这条纪律在**用量**路径上执行得很严格，但在**速率**路径上做的正是它禁止的事。

另一种说法是：告警的触发与否**对延迟不单调**。我从需求独立列不变量时，第一条就是「数据越旧，看门狗的自我评价不得越乐观」，现有套件没有这一条：

```
不变量 A：告警随落库延迟单调不减
  失败 ××  延迟 1500s 会告警，但更旧的 1800s 反而不告警
  实测：0min=静默  5min=静默  10min=静默  15min=告警  20min=告警  25min=告警
        30min=静默  35min=静默  40min=静默  45min=静默  50min=静默
```

> `node missing-invariants.mjs`（含一条对照：延迟 15 分钟必须告警——通过，证明探针不是恒失败）

**为什么变异测试抓不到它**：变异测试找的是*写错的代码*，而这里的问题是*缺失的代码*——没有一行可以被变异成「不报警」，因为本来就没有那一行。`scripts/mutation-test.sh` 结构上不可能发现这一类缺陷，加多少变异体都不行。这正是 AUDIT.md 第 5.4 节担心的那种盲区，只是它比「想象力的边界」更根本。

| | |
| --- | --- |
| **复现** | `node blind-cliff.mjs`、`node missing-invariants.mjs`、`node exp-g.mjs` |
| **实测影响** | 2 Gbps 突发、月初至今 300 GiB：L=25 分钟时**不超额**；L=30 分钟**超额 270 GiB**；L=40 分钟 **410 GiB**；L=60 分钟 **689 GiB**。全程日志只有 `OK`。 |
| **相关** | 同一条静默路径也吞掉 AUDIT.md 第 5.7 节第 1 项：若 Lightsail 返回的时间戳不是数字，`newest` 同样为 `null`，闸门的失明检测**永久**失效，症状一模一样。 |
| **方向建议** | 把「窗口内零数据点」当成一个**独立于延迟**的失明信号单独报出来，并且在没有数据支撑时不要打印 `now 0 kbps`。是否要因此 fail-closed（停机）是产品决定，但**沉默不该是选项**。 |

---

### F2 — 「设计成立的条件是 L < 约 18 分钟」偏松 6–8 分钟；`BLIND` 的「提前四分钟」实际是零

**严重 · 标定**

README 用「从空表开始的 5 Gbps 突发」作为算例，并据此告诉操作者：跑上一天看 `meter` 的实际分布，只要小于约 18 分钟就没问题。我用同一个算例，扫遍突发相对 cron 时间格的全部相位（5 Gbps 下一格 = 349 GiB，相位不是细节）：

| 落库延迟 L | 守不住配额的相位比例 | 最好情形 | 最坏情形 | BLIND 会响 |
| --- | ---: | ---: | ---: | --- |
| 6 分钟 | 0% | 647 GiB | 967 GiB | 否 |
| 8 分钟 | 0% | 647 GiB | 967 GiB | 否 |
| 10 分钟 | 0% | 647 GiB | 967 GiB | 否 |
| **12 分钟** | **33%** | 793 GiB | **1113 GiB** | 是（门槛就在这里） |
| 14 分钟 | 33% | 793 GiB | 1113 GiB | 是 |
| 16 分钟 | 67% | 909 GiB | 1230 GiB | 是 |
| 18 分钟 | 67% | 909 GiB | 1230 GiB | 是 |
| 22 分钟 | 92% | 997 GiB | 1317 GiB | 是 |

> 额度 1024 GiB。「守不住」= 断流时真实用量已越过 1024 GiB。`node exp-j.mjs`

真实的临界点落在 **10 到 12 分钟之间**，不是 README 说的「经验临界点 16 分钟」，也不是「设计成立的条件 < 约 18 分钟」。在 L = 18 分钟时，三分之二的 cron 相位会冲破配额。

连带的一条：`MAX_TOLERABLE_LAG_SECONDS = 720`（12 分钟）**恰好压在临界点上**。README 说它「比 16 分钟的经验临界点早四分钟」——那四分钟不存在。好消息是它**没有迟到**（上一轮修补想解决的正是迟到），坏消息是余量是 0 而不是 4 分钟，而整个门槛的设计理由就是「要留提前量」。

| | |
| --- | --- |
| **复现** | `node exp-j.mjs`（相位扫描）、`node exp-i.mjs` |
| **影响** | README 给操作者的验收标准是错的。按它执行，一个稳定在 15 分钟延迟的部署会被判定为「健康」，而实际上它在 5 Gbps 档已经守不住了。 |
| **注意** | 这是 5 Gbps 从空表开始这个**极端算例**的结论——README 自己选的算例。速率低时容忍度大得多。 |

---

### F3 — 突发闸门对**最坏情况**的贡献是零；`THRESHOLD` 是唯一有效的旋钮

**中等 · 架构**

详见第 5 节。摘要：在文档允许的 5 Gbps 上限内扫遍（速率 × 起始用量 × cron 相位），有闸门和无闸门的**最坏超额完全相同**：525.5 GiB。把 `THRESHOLD` 从 0.8 降到 0.5 则把它压到 210.5 GiB。

**复现**：`node exp-k.mjs`、`node exp-b.mjs`、`node exp-c.mjs`

---

### F4 — 代码注释与 README 里的闭式解是错的，且「与速率无关」的结论不成立

**中等 · 标定**

`src/index.js` 里 `BURST_WINDOW_SECONDS` 的注释声称：闸门在「距离额度耗尽还剩 `H/(H+W−L)`」处跳闸，**与速率无关**。AUDIT.md 第 5.1 节说得对——这个公式从没被数值验证过。它和它的变体都对不上：

| W | 速率 | t_exh | 实测余量 | 旧式 A：t·H/(H+W−L) | 旧式 B：(t−L)·H/(H+W−L) | 新式（已验证） |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 30 分 | 500 Mbps | 17592 s | 2592 s | 13194 s ✗ | 12744 s ✗ | 2592 s ✓ |
| 30 分 | 1000 Mbps | 8796 s | 2796 s | 6597 s ✗ | 6147 s ✗ | 2796 s ✓ |
| 30 分 | 2000 Mbps | 4398 s | 2598 s | 3299 s ✗ | 2849 s ✗ | 2598 s ✓ |
| **30 分** | **5000 Mbps** | 1759 s | **559 s** | 1319 s ✗ | 869 s ✗ | 559 s ✓ |
| **60 分** | **5000 Mbps** | 1759 s | **−41 s** | 960 s ✗ | 632 s ✗ | −41 s ✓ |
| 60 分 | 8000 Mbps | 1100 s | −100 s | 600 s ✗ | 272 s ✗ | −100 s ✓ |

> 「余量」= 跳闸时距离配额耗尽还剩多少秒；负数表示配额已经烧完才跳闸。16 组全部对上（误差 < 半个 cron 格）。`node exp-e.mjs`、`node exp-f.mjs`

**正确的形式**（λ = 有效可见延迟 = 落库延迟 + 桶对齐余数）：

```
可见窗口 = [T−W, T−λ]，长度 V = W−λ；rate_est = R·min(T−λ, V)/V

情形 1（T ≥ W，速率不再被稀释）：跳闸 T = t_exh + λ − H
                                 余量 = H − λ        ← 常数，与 R 和 W 都无关
情形 2（T < W，仍在稀释区）    ：跳闸 T = λ + (W−λ)·t_exh/(H+W−λ)
                                 余量 = t_exh·H/(H+W−λ) − λ
```

三处具体后果：

1. **「与速率无关」说反了对象。** 在慢突发那一档，*绝对余量*（≈ H − λ ≈ 45 分钟）与速率无关；README 表格里那个「跳闸时耗尽进度 40% / 63%」的*比例*则随速率剧烈变化（实测 41%–109%）。
2. **慢突发时 W 完全不起作用。** 500 / 750 / 1000 Mbps 三档，W=30 分钟和 W=60 分钟给出*逐位相同*的跳闸时刻和余量。「窗口长度换反应速度」只在快突发那一档成立。
3. **「W=60 分钟、5 Gbps 下只剩 7.2 分钟余量」的实测值是 −41 秒**——闸门在配额已经烧完之后才跳。这个数字被 README 用来论证 W 该取 30 而不是 60；*结论*碰巧是对的（30 分钟确实好），但支撑它的数是错的，而且错在把失败说成了「还有余量」。

| | |
| --- | --- |
| **复现** | `node exp-e.mjs`（对照旧式）、`node exp-f.mjs`（验证新式，16/16 对上） |
| **影响** | 不改变运行时行为。但这三段推导是 `BURST_WINDOW_SECONDS` 取 1800 的**全部理由**，任何以它们为前提的后续调参都会走偏。 |

---

### F5 — 暴露量表的「只有静态线」一列系统性偏高约 150 GiB

**中等 · 标定**

README「上游 API → 精度与延迟」那张表给的是单点值。实测扫遍 cron 相位后，**六行的单点值全部落在实测区间的上方**：

| L | README 只静态线 | 实测区间 | README 加闸门 | 实测区间 | 闸门守住配额 |
| ---: | ---: | ---: | ---: | ---: | --- |
| 5 分 | 1465 ✗ | 1049–1363 | 809 | 525–839 | 是 |
| 10 分 | 1640 ✗ | 1224–1538 | 902 | 665–979 | 是 |
| 15 分 | 1815 ✗ | 1398–1713 | 1008 | 769–1084 | 看相位 |
| 20 分 | 1989 ✗ | 1573–1888 | 1129 | 909–1224 | 看相位 |
| 25 分 | 2164 ✗ | 1748–2062 | 1269 | 1014–1329 | 看相位 |
| 30 分 | 2338 ✗ | 1922–2237 | 2338 ✗ | 1922–2237 | 否 |

> 单位 GiB。5 Gbps 从空表开始，额度 1024 GiB。`node exp-i.mjs`

「加闸门」那一列六行里有五行落在实测区间内，是个合理的中位估计。「只有静态线」那一列则一致偏高——方向上是**把闸门的价值说大了**（左右两列的差距被夸大）。表格也没有说明结果对 cron 相位有 ±350 GiB 的敏感度。

**复现**：`node exp-i.mjs`、`node exp-h.mjs`

---

### F6 — 凭据脱敏的 `replaceAll` 从未被检验

**轻微 · 覆盖**

把 `detail.replaceAll(secret, …)` 改成 `detail.replace(secret, …)`，**78 个用例在 `INVARIANT_CASES=500` 下全绿**。原因是两处夹具的错误响应体里每个凭据只出现一次，两者行为无从区分。

代码本身是对的——我用一个把 access key id 和 secret 各嵌两次的响应体验证过，残留 0 处。这是**覆盖缺口**而不是缺陷。修法是让失败夹具回显两次。

**复现**：`bash extra-mutations.sh <repo>`（变异「脱敏只替换第一处出现」→ 漏过）

---

### F7 — `sum` 的有限性检查没有夹具，且它的错误消息读作 `null`

**轻微 · 覆盖**

去掉 `!Number.isFinite(sum)` 这一项，全套测试仍然全绿——测试只生成了 `"500"`（字符串）和 `-1`（负数），没有非有限值。而 `{"sum": 1e400}` 是**完全合法的 JSON**，`JSON.parse` 会把它变成 `Infinity`。

当前行为是正确的（抛错，而且 `Infinity` 会让停机线永远被跨过，属于 fail-closed 的安全方向）。但错误消息是：

```
GetInstanceMetricData returned an unusable sum for NetworkOut: null
```

——因为 `JSON.stringify(Infinity)` 就是 `"null"`。真出问题时这行日志会把人引向错误的方向。

**复现**：`node survivors.mjs`

---

### F8 — 例子测试里没有「恰好落在停机线上」的用例

**轻微 · 覆盖**

`scheduled.test.js` 在线两侧各取了一点（±1 MiB），但没有**等于**那一点。于是 `usedGib >= limitGib` → `>` 这个变异**只**被性质测试的 boundary 分层抓住。`INVARIANT_CASES=1` 时它存活——这也正是 `npm run mutate` 的失败路径能返回 1 的原因之一（该路径本身工作正常，我验证过）。

不是缺陷，是防线只有一层。加一条 `usedGib === limitGib` 的例子测试就能让它独立于随机采样。

---

### F9 — 数字漂移：「0.011 次/秒」应为 0.0067

**轻微 · 文档**

144 次触发/天 × 4 次调用 = 576 次/天 ÷ 86400 = **0.0067 次/秒**。即便按每次触发 6 次调用的上限算也只有 0.010。README「计费与限流」一节写的 0.011 高出约 64%。方向无害（把负载说大了），但它是 AUDIT.md 第 5.6 节说的那种漂移。

这是我逐条交叉核对后**唯一**找到的明确算错的数字——README 重写之后的数字质量整体很高，详见下一节。

---

### F10 — 「1 Gbps 越线后最坏再烧约 164 GiB」既不是实测值也不是解析上界

**轻微 · 文档**

实测扫遍起始用量和 cron 相位，最坏是 **146.6 GiB**；解析上界是 速率 × (600 + 300 + 600 + 60) = **181.6 GiB**。164 落在两者之间，来源不明。

**结论本身成立**：204.8 GiB 的余量在两个数下都吃得下。只是这个数字应该换成其中一个有出处的。

**复现**：`node exp-i.mjs`

---

## 3. 核实过、没有问题的

这一节划定审计的覆盖边界。下面每一条都实际验证过，不是「看着没问题」。

### 算式与常量交叉核对

- ✓ **检测回路表全部正确**——8 个回路值（13/21、18/26、23/31、28/36 分钟）和 4 个余量比（1.43×、2.86×、1.15×、2.31×、0.97×、1.94×、0.83×、1.67×）逐个重算，全部对上。
- ✓ **cron 表全部正确**——144/720/1440 次每天，0.14%/0.72%/1.44%，576/2880/5760 次每天，17,856/89,280/178,560 次每月。（AUDIT.md 提到的 89,000 漂移已经修好。）
- ✓ **`THRESHOLD` 表全部正确**——819.2/870.4/921.6 GiB，以及两种 1 TB 解释下的 80.0%/85.9%、85.0%/91.3%、90.0%/96.6%，还有「只剩 3.4% 余量」。
- ✓ **跳闸速率正确**——空表 2.44 Gbps、用到 800 GiB 时 534 Mbps。
- ✓ **余量烧尽时间正确**——204.8 GiB 在 1 Gbps 下 29.3 分钟、2 Gbps 下 14.7 分钟。
- ✓ **16 分钟不可压缩盲区正确**——300 + 0 + 600 + 60 = 960 秒。
- ✓ **840 秒延迟预算正确**，且 `MAX_TOLERABLE_LAG_SECONDS = 720 ≤ 840` 成立（但见 F2：这个预算本身用的规则过松）。
- ✓ **决策表的调用次数全部正确**——4/3、6/5、4、4、3、2 六行逐条对着代码路径数过。
- ✓ **投影起点正确**——`PROJECTION_MIN_ELAPSED = 0.02` × 31 天 = 14.9 小时，即注释里的「约 15 小时」。
- ✓ **暴露量表的「闸门可用桶数」一列正确**——5/4/3/2/1/0。
- ✓ **源码行数** 626，README 说「约 630 行」。

### 上游 API 假设

- ✓ **时间戳确实是数字。** 官方 API 文档的 Response Syntax 明写 `"timestamp": number`。AUDIT.md 第 5.7 节把这条列为「未验证」——文档层面它是明确的（运行时仍未验证，见下一节）。
- ✓ **`metricData` 确实不是必然出现的字段。** 文档的 Response Elements 里它没有 `Required: Yes`。代码把「字段缺失」当合法空值的处理是对的，而且这是仓库里修过的最严重的一个 bug。
- ✓ **日粒度桶的对齐问题不影响结果——而且与 AWS 实际怎么对齐无关。** 这是 AUDIT.md 第 5.2 节点名的那条纯推理假设。它**由构造保证安全**：查询的 `startTime` 是 UTC 月初，而 UTC 月初本身就落在 epoch 对齐的 86400 边界上。所以「对齐到 UTC 零点」「从 `startTime` 起算」「按 epoch 向下取整」这三种可能的规则在这个查询上**给出完全相同的分桶**。不需要向 AWS 查证。
- ✓ **5 分钟是硬底。** 文档确认除 `StatusCheckFailed*`（60 秒）外所有实例指标只有 300 秒粒度，并且明写「divide the reported number by 300 to find Bytes/second」——直接支持 `bytes/(points×300)` 这个算法。
- ✓ **`period` 范围 60–86400**，与 `tuning.test.js` 的断言一致。
- ✓ **`AccessDeniedException` 在按操作那份文档里确实是 HTTP 400**，README 说的「两份文档不一致」属实。
- ✓ **`aws4fetch@1.0.20` 确实暴露 `accessKeyId` 和 `secretAccessKey`**（实例化后读出来核对过），所以脱敏循环读的属性名是对的。
- ✓ **重试判据确实只认 5xx 和 429**——源码是 `if (res.status < 500 && res.status !== 429) break`，`retries: 2` 生效。

### 行为与工具链

- ✓ **误停面比想象中小。** 对有限的合法大传输（10–100 GiB，100 Mbps–5 Gbps）扫了一遍：起始用量 0 和 400 GiB 时，100 GiB 以内的传输**在任何速率下都不会误停**。只有「已用 700 GiB 且以 ≥1 Gbps 再传 100 GiB」这类真的贴近额度的场景才跳闸——那是想要的行为。
- ✓ **单调性成立**——用量、速率、`THRESHOLD` 三个方向的单调性我独立复核过，与既有性质测试一致。
- ✓ **变异脚本自身是可信的。** 对照组正常（干净代码不报红）；`INVARIANT_CASES=1` 时确实退出 1；那时存活的两个变异恰好是性质测试负责的两个（边界等号、脱敏），说明分工是清楚的，不是检测器坏了。
- ✓ **16 个追加变异里 11 个被抓到。** 包括「fail-closed 改成 fail-open」「闸门改比到 THRESHOLD 线」「关掉 5xx 重试」「去掉 await」「`Promise.all` 改 `allSettled`」「延迟容忍抬到可观测上限」「视野翻倍」——现有套件在这些方向上防得住。
- ✓ **3 个存活的是等价变异，不是缺口**——两个方向对调（`monthIn.bytes + monthOut.bytes` 和 `rateOf(in) + rateOf(out)` 都对称）、`secondsToQuota` 的零保护改 `>=`（`x/0 = Infinity` 结果相同，且 `remaining ≤ 0` 时静态线已先行触发）。
- ✓ **工作区全程未改动**——所有变异和模拟都在 `mktemp` 副本里做，收工时 `git status` 为空，78 个用例仍然全绿。

---

## 4. 没能核实的

这台机器上既没有 AWS CLI 也没有任何 AWS 凭据（`~/.aws` 不存在，环境变量为空，无 `.dev.vars`），所以一次真实 API 调用都发不出去。

- ? **指标落库延迟的实际分布。** AWS 自己说「随服务而变」，无法从文档得到。这仍然是整套余量标定唯一一个没有文档的输入——而 F2 说明设计对它比 README 认为的敏感得多。**这是部署后第一件该量的事**，README 已经这么说了，只是验收标准要从「< 18 分钟」改成「< 10 分钟」。
- ? **运行时时间戳到底是不是数字。** 文档写的是 `number`，但没有真实响应可看。如果实际返回 ISO 字符串，`meter` 字段会静默消失，并且 F1 那条失明路径会**永久**生效。README 已经把「确认 `meter` 出现」列为部署后第一件事，这个提示是对的。
- ? **Lightsail 在 5 分钟桶完全没有流量时是否仍然发一个 `sum: 0` 的点。** 文档没说。这决定 `points` 这个分母的行为。对一台运行中的实例大概率无所谓（总有 DNS/NTP 流量），但我在模拟里把它做成了可切换项，无法定论。
- ? **`StopInstance` 打在一台已经停机的实例上返回什么。** fail-closed 路径（`GetInstanceState` 失败时照停）可能走到。代码注释假设它会被拒绝并以异常结束整轮，这是「响亮的」——但没有实证。

另外声明一句方法上的边界：我的模拟器**是一个模型**。它对上游的建模（300 秒样本、桶关闭后延迟 L 发布、日粒度按已发布样本聚合）与代码和文档一致，也通过了与被审代码的时间戳交叉校验，但它不是真实的 Lightsail。所有带具体 GiB 数字的结论都应当读作「在这个模型下」。

---

## 5. 架构：突发闸门值不值得

AUDIT.md 第 5.5 节说这是一个真正开放的问题，不要预设答案。下面是数据，然后是我的判断。

### 最坏情况：闸门贡献为零

把速率上限收到文档允许的 5 Gbps（2023-06-29 之后创建的实例），扫遍速率 × 起始用量 × cron 相位：

| 配置 | 最坏超额 | 发生在 |
| --- | ---: | --- |
| **`THRESHOLD=0.8` + 突发闸门** | **525.5 GiB** | 5 Gbps @ 815 GiB |
| **`THRESHOLD=0.8` 无闸门** | **525.5 GiB** | 5 Gbps @ 815 GiB |
| `THRESHOLD=0.7` 无闸门 | 422.0 GiB | 5 Gbps @ 100 GiB |
| `THRESHOLD=0.7` + 闸门 | 410.5 GiB | 5 Gbps @ 700 GiB |
| `THRESHOLD=0.6` 无闸门 | 322.0 GiB | 5 Gbps @ 0 GiB |
| `THRESHOLD=0.6` + 闸门 | 310.5 GiB | 5 Gbps @ 600 GiB |
| `THRESHOLD=0.5` 无闸门 | 210.5 GiB | 5 Gbps @ 500 GiB |
| `THRESHOLD=0.5` + 闸门 | 210.5 GiB | 5 Gbps @ 500 GiB |

> `node exp-k.mjs`。放开到 12.8 Gbps 时两者仍然相同：1119.8 GiB。

闸门在最坏情况上**移动了 0 到 11 GiB**；`THRESHOLD` 从 0.8 到 0.5 移动了 **315 GiB**。原因很直接：最坏情况总是「贴着静态线时来一场极快的突发」，而那时静态线在同一格 cron 里就先触发了，`burstCheck` 根本没被调用（代码里静态线短路了它）。**闸门只有在静态线还很远的时候才有机会说话，而那正是剩余额度大、闸门最不敏感的时候。**

### 中段：闸门确实做了事，但也确实误停

198 格（18 档速率 × 11 档起始用量，无限时长的突发）：

| 情形 | 格数 | 合计 |
| --- | ---: | --- |
| 闸门没跳，结果与无闸门完全一致 | 143 | — |
| 闸门跳了，真的挡下了超额 | 18 | 避免 2755 GiB 超额 |
| 闸门跳了，但本来就不会超额 | 30 | 提前掐断 5414 GiB 合法传输 |
| 闸门跳了，但仍然超额 | 7 | — |

> `node exp-c.mjs`。真正挡下超额的格子集中在 2.2–6.4 Gbps × 起始用量 0–700 GiB。

这张表对闸门是偏苛刻的：它假设突发无限持续。换成**有限的合法传输**（更贴近现实：备份、迁移），误停面小得多——100 GiB 以内的传输在起始用量 0 和 400 GiB 时任何速率都不误停。

### 判断

**闸门不是没用，但它的价值被系统性地说大了，而且它买不到用户最想要的那个东西。**

用户的第一优先级是「绝不能刷出超额账单」。在这个指标上——最坏超额——闸门的贡献是 **0**。能买到它的只有 `THRESHOLD`。README 目前的说法（「Gbps 这一档，静态线单独必定超额，突发闸门是唯一拦得住的东西」）在最坏情况下是不成立的。

闸门真正买到的是**中段的期望损失**：在 2–6 Gbps、起始用量偏低那一块，它把 18 次超额变成了不超额。代价是 30 次多余停机，以及一整套耦合（两次额外调用、观察窗口、反应视野、延迟容忍上限，以及这四者之间那些——如 F4 所示——从未被验证过的关系）。

如果要我给一个方向（这仍然是产品决定，不是审计结论）：

1. **先修 F1。** 无论闸门去留，「静默失明」都必须先处理掉——现在的失效模式是最坏的那一种：不仅不报警，还主动报平安。
2. **把 `THRESHOLD` 当成主旋钮。** 它是唯一能改善最坏情况的东西，而且 README 自己已经论证过「静态线触发不算过早停机」——降它的代价比闸门误停的代价小得多。
3. **然后再问闸门值不值得。** 如果 `THRESHOLD` 降到 0.6，静态线在 614 GiB 就响，闸门能说话的窗口进一步变窄，那套耦合的性价比会更低。而 README 自己那条 WARNING 说得对：真要防 Gbps 级别，正确做法是在实例前面加限速。

---

## 6. 复现

全部脚本在 `audit-evidence/`（未纳入 git）：

```
audit-evidence/
├── sim/                     离散事件模拟器（真实 handler + 模拟指标管道）
│   ├── harness.mjs          管道模型；harness-nogate/-w1800/-w3600 是对照变体
│   ├── control.mjs          模拟器自检（2 对照组 + 2 反向 + 2 刻度）
│   ├── probe-lag.mjs        交叉校验：注入的 L vs 代码实测的 meter
│   ├── exp-a/-b/-c/-d.mjs   闸门 vs 无闸门、最坏超额、收益/代价、有限传输误停
│   ├── exp-e/-f.mjs         闭式解证伪 / 新式验证（F4）
│   ├── exp-g/-h/-i/-j.mjs   延迟扫描、README 表核对、相位扫描、临界延迟（F1/F2/F5）
│   └── exp-k.mjs            5 Gbps 上限下的最坏超额（F3）
├── repro/
│   ├── blind-cliff.mjs      F1 的最小复现（不依赖模拟器）
│   ├── missing-invariants.mjs  我独立列的不变量，含对照
│   └── survivors.mjs        F6/F7 的存活变异刻画
└── extra-mutations.sh       16 个追加变异，全程在 mktemp 副本里跑
```

跑之前先跑 `control.mjs`——如果自检不过，后面的数字都不要信。

```bash
cd audit-evidence/sim && node control.mjs
```

```bash
cd audit-evidence/repro && node blind-cliff.mjs
```

```bash
bash audit-evidence/extra-mutations.sh "$PWD"
```

> `sim/` 与 `repro/` 里各有一份 `src/index.js` 的快照（审计时的 `d54b2ed`）和指向仓库 `node_modules` 的符号链接。代码变了之后要重跑，需要把快照换成新版本。

---

*独立审计 · na-lightsail-monitor @ `d54b2ed` · 2026-08-22*
*未提交任何改动。报告中的每个数字都标注了产生它的脚本。*
*「78 个测试通过」不是本报告任何一条结论的依据。*
