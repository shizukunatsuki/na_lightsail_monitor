# 跨模型审计指引

> 这个项目的代码、测试、文档全部由 Claude 写成；随后的一轮独立审计**也是 Claude**。
> 找你来，是因为同族模型可能共享盲区——那一轮审计确实找到了真缺陷，但它和被审对象出自
> 同一个模型家族，凡是两边都「想不到」的东西，谁也不会发现。
>
> **你的价值不在于比它们更仔细，而在于你和它们不一样。**

---

## 0. 先看这个：最可能的共享盲区

如果你只有精力查一件事，查这个。

代码里判断「最近的流量速率」时，向 AWS Lightsail 查 `GetInstanceMetricData`（`period: 300`，窗口 30 分钟），然后用

```
速率 = 返回的字节总数 / (返回的数据点个数 × 300)
```

这背后有一整套关于**指标管道**的假设，而**上一轮审计的模拟器、以及被审代码作者的所有探针，用的是同一套假设**：

1. 底层样本按 5 分钟一个桶，桶起点 `T` 覆盖 `[T, T+300)`
2. 桶必须先「关闭」（走完 300 秒），再经过一段落库延迟 `L`，才可被查到
3. 查询 `[start, end]` 返回**起点落在窗口内**的桶
4. 没有流量的 5 分钟里，AWS **不发**数据点（而不是发一个 `sum: 0` 的点）
5. 落库延迟对所有桶大致一致

**这五条没有任何一条对真实 API 验证过**——审计报告自己也在「没能核实」一节里承认，那台机器上没有 AWS 凭据。整个项目最核心的两个结论（「反应视野取 60 分钟」「落库延迟必须小于 10 分钟」）全都建立在它们之上。

请优先做两件事：

- **用你自己的知识独立判断这五条**。你对 AWS CloudWatch / Lightsail 指标行为的了解可能和 Claude 不同。特别是第 3 条和第 4 条——它们直接决定「数据点个数」这个分母是否有意义。
- 如果有任何一条你认为可能不对，**说清楚它错了会怎样**。比如：如果 AWS 对空闲的 5 分钟也发 `sum: 0` 的点，那么「窗口里零数据点」就只可能意味着落库延迟，代码里那个花一次额外调用去查实例状态的分支就是多余的；反过来，如果 AWS 返回的是滑动窗口而不是对齐的桶，那么分母就完全错了。

---

## 1. 项目是什么（30 秒）

一个 Cloudflare Worker，cron 每 10 分钟跑一次，查 AWS Lightsail 实例本月已用流量，超线就停机，下个月额度重置后再启动。防的是 Lightsail 没有上限的流量超额账单。

两道停机线：

- **静态线**：月度总量 ≥ 额度 × `THRESHOLD`（默认 0.8）
- **突发闸门**：按最近 30 分钟的速率算，剩余额度撑不过 60 分钟（`REACTION_HORIZON_SECONDS`）

核心文件 `src/index.js`，约 650 行，无状态。其余是测试和文档。

**用户的优先级**（按此权衡，不要按代码美观）：

1. 绝不能刷出超额账单（漏停 ≫ 多停）
2. 也不希望被过早停机
3. 不希望高频调用上游被判定为滥用（这是 cron 取 10 分钟的唯一理由）
4. 适用范围锁定「整个 AWS 账号只有一台长期存在的实例」

---

## 2. Claude 这一层可能有的系统性偏差

这些不是指控，是给你的搜索方向。请带着「如果这是一个语言模型的典型毛病，它会长什么样」去看。

**a. 用「解释得很充分」冒充「验证过」。** 代码注释和 README 里有大量结构良好、读起来很有说服力的推导。上一轮审计已经证伪了其中一个核心闭式解——它写得很漂亮，也很错。**请把「写得越确信的地方」当成优先怀疑对象**，尤其是任何以「可以证明」「显然」「必然」开头的句子。

**b. 加层而不是减层。** 这个项目有：两道停机线、一个观察窗口、一个反应视野、一个延迟容忍上限、四层测试、一个变异测试脚本。上一轮审计已经量化出突发闸门对**最坏情况**的贡献接近零（约 10–15 GiB，而调 `THRESHOLD` 是 300+ GiB），但结论仍是保留它。**请独立判断：这套复杂度是必要的吗？有没有明显更简单的等效方案？** 比如——为什么不干脆把 `THRESHOLD` 调低、把闸门整个删掉？为什么不用 Lightsail 自带的指标告警加自动化动作？

**c. 在同一个框架里打转。** 「静态线 + 突发闸门」「停机路径 fail-closed / 重启路径 fail-open」这些框架从项目早期就定下来了，之后所有讨论都在框架内部。**请退一步问：这个问题本身有没有别的解法？** 例如：能不能不做「停机」而做「限速」？能不能把判断放在实例内部（一个 cron 脚本读本机网卡计数器）而不是从 AWS 侧远程观测——那样就没有落库延迟这回事了。

**d. 对 AWS 文档的同一种读法。** 两轮 Claude 引用的是同一批文档页。你的训练数据可能覆盖不同的版本或不同的社区经验。**凡是文档里没写死的地方**（落库延迟、空桶行为、限流阈值），你的判断可能不一样，请说出来。

**e. JavaScript 层面的坑可能被「业务视角」盖过去。** 两轮审计都在讨论账单和速率，很少有人把 `src/index.js` 当成一段**普通 JS 程序**逐行读。请专门做一遍这个：类型强制转换、浮点、`async` 顺序、`Promise` 拒绝的吞没、`??` 与 `||` 的区别、数值溢出、时区、闭包捕获。

---

## 3. 已经做过什么（以及为什么你仍然该怀疑）

这一节是为了让你不做重复劳动，**不是为了让你相信这些结论**。

- **例子测试 + 性质测试 + 调参耦合检查 + 变异测试**，86 个用例，行/分支/函数覆盖率 100%，24 个注入缺陷全部被测试抓到。
  → 但覆盖率和变异测试都**抓不到「缺失的代码」**。项目里最严重的那个缺陷（某条路径上静默失明）正是这一类，是靠人从需求独立列不变量才发现的。
- **上一轮审计写了一个离散事件模拟器**，量出了「真正断流时实际烧掉多少字节」，并用它证伪了一个闭式解、量化了突发闸门的价值。
  → 但那个模拟器的管道模型就是第 0 节那五条假设。**模型对了结论才对。**
- **AWS 文档核对**：响应字段类型、空响应的两种长相、错误状态码、计费与配额、5 分钟粒度是硬底。
  → 都是文档层面的核对，**没有一次真实 API 调用**。

**没有被任何人验证过的**（按重要性）：

1. 真实的落库延迟分布（AWS 官方说「随服务而变」，不公开）
2. 真实响应里 `timestamp` 是不是数字（文档写 `number`，但没见过真实响应）
3. 空闲 5 分钟是否仍发 `sum: 0` 的数据点
4. `StopInstance` 打在一台已停机实例上返回什么

---

## 4. 待验证的具体断言

如果你能跑代码，直接实验。**如果你只能读代码和文本，下面这些数字全部可以用纸笔核**——请逐条核算，算错的地方就是发现。

| # | 断言（出自代码注释或 README） | 怎么核 |
| --- | --- | --- |
| 1 | 可观测延迟的天花板是 `窗口 − 粒度 = 1800 − 300 = 1500` 秒 = 25 分钟 | 桶起点 `T` 在 `T+300+L` 可查；要落在窗口 `[now−1800, now]` 需 `T ≥ now−1800`。解这个不等式 |
| 2 | 检测回路 = 桶关闭(≤300) + 落库延迟 + cron(600) + 停机生效(60)；延迟按 600 算得 1560 秒 = 26 分钟 | 直接相加 |
| 3 | 反应视野 3600 秒对 1560 秒回路是 2.31 倍余量 | 除法 |
| 4 | 延迟容忍上限的推导：`3600/2 − (300+600+60) = 840` 秒，取 720 | 算术 |
| 5 | 跳闸余量的闭式解：非稀释区 `H − λ`；稀释区 `t_exh·H/(H+W−λ) − λ` | **请独立重推一遍**。注意「月度用量读数本身也滞后 λ」——上一版公式正是漏了这一项 |
| 6 | `THRESHOLD` 表：`1024×0.8 = 819.2 GiB`；若 1 TB = 1024×10⁹ 字节则占 85.9% | `819.2 × 2^30 / 1.024e12` |
| 7 | cron 表：144 次/天、576 次调用/天、17,856 次/月、0.0067 次/秒 | 算术 |
| 8 | 「1 Gbps 越线后最坏再烧 181.6 GiB」是解析上界 | `1e9 × 1560 / 8 / 2^30` |
| 9 | 「设计成立的条件是落库延迟 < 约 10 分钟」 | 这是上一轮审计用模拟器扫相位得到的。**请判断这个结论对第 0 节的假设有多敏感** |
| 10 | 单位推断：`transferPerMonthInGb: 1024` 表示 GiB 而非 GB | 论证说「即便推断错了默认配置依然安全」。**请检查这个安全性论证本身是否成立** |

---

## 5. 具体请你做的事

按优先级：

1. **第 0 节的五条管道假设**——用你自己的知识判断，逐条给「同意 / 不同意 / 不确定」，不同意的说清后果。
2. **把 `src/index.js` 当普通 JS 程序逐行读一遍**，找语言层面的坑。别用项目自己的框架去看它。
3. **第 4 节的十条断言逐个核算。**
4. **架构质疑**：这套复杂度必要吗？有没有明显更简单的等效方案？特别是——把判断放在实例内部而不是从 AWS 侧远程观测，是否从根本上绕开了落库延迟这个最大的不确定性？
5. **找「缺失的代码」**：不要问「这行写对了吗」，问「有没有一整类情况根本没有被处理」。已知的一次教训是：某条路径上「没有数据」被当成了「数据是零」。**还有别的地方犯同样的错吗？**
6. 顺带看一眼 `wrangler.jsonc`、`.github/workflows/test.yml`、`package.json` 和那份 IAM 策略——它们几乎没被审过。

---

## 6. 交付

一份报告，包含：

1. **发现清单**：每条给出定位（文件 + 行）、为什么错、后果（会导致漏停还是误停）、你的严重度判断。
2. **你核实过但认为没问题的清单**——划定覆盖边界。
3. **你不同意上一轮审计或不同意现有设计的地方**，即使你无法证明。**这一条对我们最有价值**：它正是跨模型审计的意义所在，哪怕只是「我会用另一种方式做这件事」。
4. **你没能核实的清单**及原因。

**请不要把「测试通过」「覆盖率 100%」当成任何结论的依据**，也不要因为某处注释解释得很充分就跳过它。请说明你实际验证了什么、用什么方法。

---

## 7. 不要做的

- 不要重新论证这些已经拍板的事：只支持单实例；cron 用 10 分钟；没有运行时的关闭保护开关；不主动发通知（靠 AWS Budgets + Lightsail 原生告警补）；文档中文、日志英文。
- 不要为了让测试变绿而改测试期望——先判断是实现错了还是期望错了。
- 不要直接提交改动。发现和改动分开。
